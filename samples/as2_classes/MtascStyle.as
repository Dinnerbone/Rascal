class MtascStyle {
    public static function main() {
        trace("Main method!");
        trace(AnEmptyInterface(foo));
        trace(NotAnInterface(foo));
        trace(path.to.ANestedInterface(foo));
    }
}
